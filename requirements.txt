# MathViz Unified - runtime dependencies
# Install with:  pip install -e .        (dev: pip install -e ".[dev]")
fastapi>=0.104
uvicorn[standard]>=0.24
pydantic>=2.5
python-multipart>=0.0.6
jinja2>=3.1
manim>=0.19,<0.21
# System requirement (not pip-installable): ffmpeg on PATH.
# Windows:  winget install ffmpeg     (or: choco install ffmpeg)
# Ubuntu:   sudo apt install ffmpeg
# macOS:    brew install ffmpeg